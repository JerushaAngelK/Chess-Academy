import React from 'react';

const ProgressTracker = ({ progress }) => {
  return (
    <div className="progress-tracker">
      <h3>Progress</h3>
      {/* Implement the UI for tracking student progress */}
    </div>
  );
};

export default ProgressTracker;